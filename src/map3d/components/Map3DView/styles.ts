import { styled } from '@mui/material/styles';

export const MapContainer = styled('div')({
  position: 'relative',
  overflow: 'hidden',
  width: '100%',
  top: 0,
  left: 0,
  height: '100vh',
  cursor: 'default',
});